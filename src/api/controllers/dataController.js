export default class DataController {
	static async apiGetSearchFormData(req, res, next) {
		try {
		} catch (err) {
			console.error(`Unable to get search-form data in DataController: ${err}`);
		}
	}
}
